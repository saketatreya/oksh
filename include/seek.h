#ifndef SEEK_H
#define SEEK_H

// Function to execute the seek command with flags and arguments
void seek_command(const char *target, const char *directory, int show_dirs, int show_files, int execute_flag);

#endif // SEEK_H
